<Window x:Class="WowMapExplorer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ad="clr-namespace:AvalonDock;assembly=AvalonDock"
        xmlns:menu="clr-namespace:WowMapExplorer.Menus"
        xmlns:v="clr-namespace:WowMapExplorer.Views"
        Title="WowMapExplorer"
        Width="800"
        Height="600"
        Icon="{StaticResource WowIcon}">
    <DockPanel LastChildFill="true">
        <!--  menu  -->
        <menu:MapMenu x:Name="_mapMenu" DockPanel.Dock="Top" />

        <!--  docking manager  -->
        <ad:DockingManager x:Name="_dockManager" IsAnimationEnabled="True">
            <ad:ResizingPanel FocusManager.IsFocusScope="True" Orientation="Horizontal">
                <ad:DockablePane x:Name="_leftDockablePane" ad:ResizingPanel.ResizeWidth="0">
                    <v:MapContent x:Name="_mapContent" />
                </ad:DockablePane>

                <!--  Main Content  -->
                <ad:DocumentPane Name="_documentPane" ShowHeader="False">
                    <ad:DocumentContent IsCloseable="false">
                        <Border Name="_controlHostElement" />
                    </ad:DocumentContent>
                </ad:DocumentPane>
            </ad:ResizingPanel>
        </ad:DockingManager>
    </DockPanel>
</Window>
